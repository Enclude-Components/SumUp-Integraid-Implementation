minimum_cumulusci_version: '3.90.0'
project:
    name: SumUp-Integraid-Implementation
    package:
        name: SumUp-Integraid-Implementation
        api_version: '61.0'
    dependencies:
        - github: 'https://github.com/EncludeLtd/Integraid'
    git:
        default_branch: 'main'
    source_format: sfdx

tasks:
    run_tests:
        options:
            required_org_code_coverage_percent: 75
